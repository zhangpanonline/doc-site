---
sidebar_position: 13
title: '13. 装饰器'
description: "Python 装饰器：高阶函数与语法糖，函数装饰器与带参数装饰器的完整写法。"
displayed_sidebar: agentsPython
---

# 装饰器

## 装饰器的本质

装饰器本质上是一个接受函数作为参数并返回新函数的高阶函数：

```python title="装饰器本质示例"
def my_decorator(func):
    def wrapper():
        print("函数执行前")
        func()
        print("函数执行后")
    return wrapper

# 下面的代码
def say_hello():
    print("Hello!")
say_hello = my_decorator(say_hello)

# 等效于
@my_decorator
def say_hello():
    print("Hello!")

say_hello()
# 输出：
# 函数执行前
# Hello!
# 函数执行后
```

:::note 装饰器的本质

装饰器是一个可调用对象，接收一个可调用对象，返回任意对象。但为了保证程序能正常运行，通常返回另一个可调用对象来替代原对象。

:::

---

## 多个装饰器叠加

可以同时使用多个装饰器，执行顺序为从下到上：

```python title="多个装饰器叠加"
@decorator_a
@decorator_b
def func():
    pass

# 等效于：
# func = decorator_a(decorator_b(func))
```

---

## 🎓 互动教学课程

想检验自己是否真的掌握了本章内容？

<a href="/teach/lessons/0013-装饰器.html"><strong>👉 点击进入《装饰器互动教学课程》</strong></a>

- ⏱️ 预计 10–15 分钟：知识速览 + 9 道分级测验（初/中/高级各 3 道）+ 面试实战 + 4 道源课程作业（timer/wraps）改编的最终考核
- 📋 配套<a href="/teach/reference/装饰器速查表.html">《装饰器速查表》</a>，可打印
- 💬 有不理解的地方，随时可以问你的 AI 老师
