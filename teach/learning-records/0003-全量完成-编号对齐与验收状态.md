# 30 章全量生成完成：编号对齐方案落地，全章节校验通过

2026-09-05 全部 30 章的互动教学课程生成完毕。关键决策与结果：

**编号方案**：lessons 编号 = 站点章节号（0001-基础语法 → 0030-断点调试）。原协程课程（0001）重编号为 0024-协程 并升级为新格式（补源作业 gather/Event 最终考核 + demo 内嵌）；原事件循环样板（0002）重编号为 0022-事件循环 保留。

**每章结构**（统一模板）：本课目标 → 知识速览（引用官方文档/Real Python）→ 基础测验 6 题 → 最终考核 4 题（源作业改编：预测类转选择题、编码类转「选实现」+ 参考答案折叠）→ 巩固延伸 → AI 老师课尾。每章配 reference 速查表 + 文档末尾 `<a>` 入口。

**代码校验（用户强制要求）**：30 个 verify 脚本全部实际运行通过（$CLAUDE_JOB_DIR/tmp/verify_*.py）。校验中发现并修正的教学错误：0016 思考题1 的输出（异常在 yield 处抛入，yield 后代码被跳过）；0012 浅拷贝陷阱的讲解（merge_data 会修改原 data 的嵌套 b）；0029 uv tree 无 (workspace) 标记（站点文档示例与 uv 0.12.5 实际行为不符）；0030 settrace 首事件是 call 而非 line。此外 27-29 章用 uv 做了真实构建/workspace 复现验证。

**特殊章**：26.多线程和多进程源课程无作业 → 按计划出等效考核（竞态/Lock/GIL/死锁，真实运行 20 次试验验证）。

**Implications**: 用户验收后如需调整模板（题量、考核形态、样式），按 NOTES.md 的模式批量修改即可；后续新增章节按同样六步流程执行。
